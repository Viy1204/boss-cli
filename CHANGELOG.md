# 更新日志

本文件从 0.8.0 开始记，更早的版本看 git tag 和 GitHub Releases。

只记**会改变你使用方式的东西**：行为变更、新参数、以及那些「看起来一直在工作、其实从来没生效」的修复。
纯内部重构不记。

## 0.8.1

### 修复

- **侧栏导航整段没在工作。** `clickBossSidebarMenuToPath` 里的点击拿到的是个函数对象而不是布尔值，
  于是「未找到菜单」永远不报错、而点击其实根本没发生；紧接着的「等待跳转完成」判据同样恒为真，
  直接 return，连 `page.goto` 兜底都跑不到。整个函数等于什么都没做还报成功。影响 `boss jd`。
- **`boss recommend <岗位>` 切岗位后不等列表渲染就往下读**，同一个原因（等待空转）。
- **`boss action remark` 的「备注输入未生效」校验从来没触发过**，同一个原因。

三处根因相同：puppeteer 对**字符串形式**的 pageFunction 是当表达式求值的，
`evaluate(脚本, 入参)` 后面的入参会被静默丢弃，求值结果是个函数对象——当判据恒为真，当返回值恒为真值。
全仓库已清干净，写法约定见 `AGENTS.md`。

## 0.8.0

### ⚠️ 行为变更（相对 0.7.x）

- **`boss search` 每次都会先点一次「清空筛选」**，再按本次参数重设。
  筛选条件存在平台侧、原本跨命令粘着：上一条命令设过的学历/院校等会继续生效，
  而结果标题打的是入参，所以界面上看不出来。现在这一轮生效的条件只有你这条命令里写的。
- **`boss search` 不传 `--job` 会切「不限职位」**，不再沿用平台上残留的岗位。
  原来同一条命令跑两次可能搜的是两个池子。
- 结果标题里的每一项都改成**从页面实时读**，不再回显入参——标题里没写的条件就是真没生效。

### 新增

`boss search` 补齐了平台侧的筛选项：

| 参数 | 说明 |
|---|---|
| `--city` | 搜索城市，默认读环境变量 `BOSS_SEARCH_CITY` |
| `--degree` / `--degree-range` | 学历预设档位 / 自定义区间（如 `大专-本科`） |
| `--school` | 院校要求，可多选 |
| `--exp` / `--exp-range` | 经验预设档位 / 自定义区间（如 `3-8`） |
| `--age` / `--age-range` | 年龄预设档位 / 自定义区间（如 `23-27`） |
| `--status` | 求职状态，可多选 |
| `--job-hop` | 跳槽频率 |
| `--major` | 专业，可多选，最多 10 个 |

`-range` 系列与对应的预设参数互斥（页面上本来就是二选一）。取值和注意事项见 README 的「搜索筛选条件」。

### 修复

- **`--city` 的城市匹配一直是坏的**：选项匹配拿不到入参，恒为空对象，每次都报「城市查无此项」；
  只有城市本来就是目标值、走早退分支时才像是好的。

### 已知不支持

平台上的「性别」「薪资区间」「牛人活跃度」「牛人职位要求」没做；「资格证书」平台自身是隐藏的。
